|                                                                                                                                                                                                                                                                                     |     |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| 用户希望后续进行 LeetCode Hot100 / 类似难度题目的 Python 手撕面试复习时，采用循环诊断系统：不是按顺序刷题，而是根据掌握情况动态出题；不会的题要记录卡点并反复回炉直到会；用户时间有限（1-2天），可能大量遗忘，需要启发式教学；每题都提供 ACM 标准答案和 input 格式；用户也会插入非 Hot100 但类似难度题，也要纳入同一循环；每次没做出来都要记录卡点，并对比每次完成水平。用户偏好：先让其尝试，必要时逐步启发，不直接一上来灌完整解法。每轮不要只给 1 题；至少一次给 5-10 题，不固定题量，可动态混合题型。. |     |

|   |   |
|---|---|
|用户当前在准备前端/React 面试时，正在通过自己项目中的真实 AI 生成代码来补 React 和前端工程理解。用户目前对语法细节不太熟悉，容易被长代码吓到，希望先建立宏观结构感，再逐步追问具体语法。当前已学习并基本理解：useState 是页面状态；useEffect 用于处理请求接口、DOM 操作、定时器、事件订阅等副作用；模板字符串 `${}` 用于字符串拼接；数据集、字段信息、GeoJSON、危险区、单点时序之间的区别。用户更需要“尊重源码”的宏观项目讲解和面试表达，而不是过早深挖细枝末节。用户当前 InSAR 页面项目理解进度：页面后端数据可理解为数据集（选哪份工程数据）、字段信息（这份数据有哪些列）、GeoJSON（地图上要画的带经纬度点位）、危险区（一堆风险点聚合出来的区域）、单点时序（点击某个点后的历史变化曲线）。用户希望后续在新窗口继续时，能从这些进度接上。.||

|   |   |
|---|---|
|用户补充了华为文档扫描项目中的真实日常问题：1）LLM 幻觉类误报，例如“Windows主机连接时间过短”被建议“连结应改为连接”，或者“账户/帐户”这类格式一致但风格差异被误报；2）英文模式下出现奇怪误报，如“xxxxx. ”被提示“句尾句号前不应有空格，请删除”，疑似英文标点/空格规则或 prompt/后处理问题；3）编排失败类问题：扫描出了错误项，但相关 Excel 文件无法下载；4）内部 bug：把 Excel 扫描结果的一坨 chunk 直接塞进异常信息；5）性能问题：内部速率约 80 个任务需要几个小时。用户澄清华为项目中前两个误报并非普通风格差异或模型能力不足：例如“Windows主机连接时间过短”这类原文本身已经是正确写法，但系统仍按固定格式报错“连结应改为连接”，属于无中生有式幻觉/错配，即模型或后处理生成了“把错误词改为正确词”的建议，但原文中并不存在错误词；英文句号空格类也可能是类似位置/证据错配问题。用户强调内部使用较强模型（如 Qwen3 100B 以上）也不应简单归因于模型太弱，更应该从证据绑定、问题定位、prompt/后处理链路、chunk 与原文对齐等角度解释。用户澄清华为文档扫描项目中“定位”链路并不是动态生成的：chunk 本身有字段绑定文件，spanitem 绑定 chunk，因此错误不是简单的文件/chunk 定位丢失。后续分析“原文正确但报错建议无中生有”时，应考虑 chunk 内部证据、spanitem 内容、LLM 输出结构/后处理映射、规则模板、缓存/并发串扰等更细链路。.||

|   |   |
|---|---|
|用户时间有限，后续回答面试项目/技术方案类问题时偏好：直接给结论、判断是否值得讲；必须落到具体工程流程、边界条件、数据结构/规则/伪代码和面试可背版本；不要只讲抽象方向、不要炫耀技术路线、不要回答一半。.||

|   |   |
|---|---|
|用户提到华为实习里有一个“非完全标准的 agent”项目，本质是基于 FastAPI + MySQL + 线程池 + 多文档解析 + LLM 的文档质量检查系统，采用固定流程和二次 Agent 校验，不是标准 ReAct Agent。流程包括 FastAPI 接收任务、线程池异步执行、MySQL task/文档任务表更新进度、前端查询进度；文档按类型 chunk 化后做 common check 高召回分析，再进行基于证据的二次筛选。系统识别标点、语法、链接验证、内容合规安全、账号密码泄露、不规范/错误用语等问题。对于不规范用语 5005，用户设计 v2：构建证据 + 打分机制 + 灰色区域 LLM 二次校验，依据出现频率、跨文档分布、标题出现、定义句出现等判断新术语/白名单/报错。对于疑似密码问题，主要靠 prompt 和上下文区分示例代码、占位符、明显泄露。对于 URL 验证，采用类 ReAct 循环，不靠大量 if-else 写死；进入循环前仅做锚点、401 等硬兜底，循环内由 LLM 根据 observation 选择工具，包括语义匹配、跟随跳转、探测浏览器请求头、标记结果等，最终输出存在/不存在/模糊。URL failure modes 包括明确不可达、访问受限、中间页跳转、资源迁移页、JS 渲染不足、资源类型不匹配、上下文不匹配等。.||

|   |   |
|---|---|
|用户刷题时容易被题目中“最大/最小”的表述干扰，希望后续讲解重点强调：不要只看最大/最小字眼，而要判断答案变量、可行性 check、以及单调方向。.||

|   |   |
|---|---|
|用户在刷题讲解中希望重点补足排列组合建模，尤其是“先选值，再选位置”“有序/无序是否重复计数”等步骤，需要把组合计数讲得更慢更清楚。.||

|   |   |
|---|---|
|用户希望后续刷题讲解遵循固定格式：先抓题目触发词/条件反射，再讲数据结构设计和表示，最后给 Python 实现。.||

|   |   |
|---|---|
|用户正在参与 reliability_test 项目的重构工作，需要对“原有组件化故障注入测试工具结构”与“新可能的场景/任务驱动平台化结构”进行对比理解，包括能力层、抽象层、编排层、表现层等架构维度的映射与分析。.||

|   |   |
|---|---|
|用户在华为实习项目中，最近参与并完成了一项“多类型文档的链接提取”工作；其中 PDF 处理最难，最后采用了妥协方案。由于保密协议限制，用户无法直接拿出源代码，后续需要基于可公开描述的信息来整理项目表达与面试材料。.||

|   |   |
|---|---|
|用户表示可以参考朋友已成功面试的简历思路来改写自己的简历与项目表述，并提供了一份已成功面试同学的简历作为参考样本，后续希望借鉴其“项目包装、亮点提炼、AI/Agent协作表述、工程复杂度表达”的写法思路来改写自己的简历与项目经历。.||

|   |   |
|---|---|
|用户还提到“杨高中路”网站实际复杂度比简历写得更高，后续希望借助 AI 深挖其中的工程亮点。.||

|   |
|---|
|用户正在准备字节客户端/前端面试；其主技术栈是 React + TypeScript，不是 Vue。用户希望以“打磨项目表达、深挖项目亮点、包装实习经历”为主要准备方向，而不是突击背前端八股。.|

|   |
|---|
|用户是中国人，阅读英文术语时会有明显理解负担。后续回答尤其是代码、前端、算法、项目讲解时，如果出现英文术语、代码参数名、函数名、类名、变量名、库名、框架概念等，即使只出现一小段，也要尽量用中文解释其含义、作用、使用场景和上下文；不要默认这些英文名对用户“无所谓”。用户希望回答更照顾中文语境下的理解成本。|

|   |
|---|
|字节面试复盘与当前问题判断：用户近期参加字节客户端/前端方向面试。整体结果可能不通过，但有正向信号：项目描述相比之前已经明显更清楚，面试官能听懂项目背景和流程，并继续深入追问工程化细节。这说明当前主要问题不是“项目完全讲不清”，而是项目表达与简历呈现还没有体现足够强的工程系统能力。这次面试暴露出的核心问题：1）面试官开始追问工程化可信度，而不是只问业务流程，例如 ReAct 内部报错能否审计、有没有日志/trace、是不是工厂模式、项目可靠性怎么评估，是人肉排查还是 case；2）这些问题背后是在判断项目是否具备可观测性、可审计性、可扩展性、可回归性和工程主导感；3）用户意识到简历项目目前“不够些”，不是经历不够，而是工程闭环表达不足；4）用户补充：同恩那段项目如果只写“解决小 bug”意义不大，不应该作为核心亮点，后续同恩经历需要谨慎处理，不能堆琐碎 bug 修复，而要么提炼成有系统价值的工程点，要么降低权重；5）用户希望后续不要马上改简历，而是先明确面试官真正想看什么、哪些项目值得升级、代码层面要补哪些工程结构、简历中应该突出哪些工程系统能力。当前判断：用户的项目讲述已经进入“能被追问”的阶段，下一步重点不是单纯润色，而是把华为 AI 文档扫描项目、杨高中路项目、同恩经历中可用的部分，重构成有工程问题、结构化方案、可验证收益的项目叙事。|

|   |
|---|
|面试项目表达的强约束：用户后续准备前端/客户端/字节类面试时，项目表达必须满足以下强约束：1）不能只讲“我做了什么功能”，不要只说实现页面、接接口、做图表、做文档解析、接 LLM、写检查规则，必须升级为遇到什么工程问题、设计了什么结构化方案、带来了什么收益或可验证改进；2）项目必须体现工程系统能力，面试官真正关心的是项目是不是一个可交付系统，而不是 demo 或脚本，后续项目表达应尽量体现可观测性、可审计性、可扩展性、可回归性、可稳定运行和可协作维护；3）AI/LLM 项目不能只强调“用了大模型”，对于华为 AI 文档扫描项目，后续不能只讲 LLM、Agent、prompt、ReAct，必须强调 LLM 只是检查能力的一部分，外层有任务编排、文档解析、chunk/span 证据绑定、结构化审计、误报回归、结果导出等工程链路，面试重点是“AI 工程系统”，不是“调了一个模型”；4）面试官追问时，要优先回答工程抓手，例如问 ReAct 报错就回答 trace / action / observation / tool output / error / replay，问有没有日志就回答 task log / checker log / tool log / result log，问是不是工厂模式就回答 ParserFactory / CheckerFactory / ToolRegistry，而不是背设计模式定义，问可靠性评估就回答 golden cases / false positive cases / failure-mode cases / regression / precision / false positive rate；5）简历项目必须经得起追问，简历上的每个亮点都应该能继续展开为什么做、原方案有什么问题、怎么设计、数据结构是什么、边界情况是什么、怎么测试、怎么证明有效、出错怎么定位，如果答不出来，就不要轻易写得太大；6）同恩经历不能写成“解决小 bug”，用户已经意识到同恩项目如果只是描述“修小 bug / 做小需求”没有意义，后续要么提炼成前端工程化、三维状态管理、图层/材质配置、可视化系统协作等有价值问题，要么在简历中降低权重，不作为核心竞争力。|

|   |
|---|
|同学模板简历的内容与特色：用户有一份参考模板简历《260321-后端.pdf》，是一位同学的后端方向简历。用户希望后续升级自己的简历时参考这份简历的表达方式，而不是照抄技术栈。这份同学简历的主要特色：1）每段经历都不是简单堆功能，而是突出“工程问题 → 结构化方案 → 结果收益”；2）表达方式更像工程师，而不是学生项目罗列；3）每个 bullet 通常包含明确的工程抓手，例如分散 Crontab 任务迁移到 Celery 分布式任务队列、大文件上传不稳定则实现分片上传提高传输稳定性、多数据库连接器分散则使用工厂模式统一 MySQL/PostgreSQL 连接器接口并保留差异化处理、测试框架老旧/不兼容则升级 Cypress 解决 ARM 兼容性并提升测试运行效率、AI 协作系统不是笼统说 Agent，而是描述 Agent 作为 peer 加入协作房间并复用实时协作基础设施；4）这份简历的优势不是“技术名词多”，而是让面试官能快速看到候选人解决了真实工程问题、有抽象能力、有重构能力、有性能/稳定性/扩展性意识、能把项目讲成一个系统而不是一堆功能；5）用户自己的简历目前也有复杂项目，如华为 AI 文档质量扫描系统、杨高中路隧道监测数字孪生系统、同恩三维/物联网/全栈相关经历，但用户自己的表达目前更偏“做了哪些模块”，还不够像同学简历那样把问题、方案、收益打穿；6）后续升级用户简历时，应借鉴同学简历的“工程叙事结构”，而不是机械模仿后端技术栈；7）尤其要学习同学简历中这些表达特点：用动词开头，如重构、设计、迁移、抽象、优化、接入、治理；不孤立写技术名词，而是写技术解决了什么问题；尽量给出结果，如提升稳定性、降低维护成本、减少重复代码、加快测试速度、提高成功率；每个项目都要有一两个能被追问的“硬点”。|

|   |
|---|
|当前简历/项目升级思路：用户当前目标是升级前端/客户端/字节方向简历与项目表达，但当前阶段不要急着直接改简历。应先明确项目升级方向、代码补强点、面试表达主线，再决定哪些内容写进简历。当前最重要的升级思路包括：1）华为 AI 文档扫描项目要从“LLM 应用”升级成“AI 文档质量扫描工程系统”，不要只讲 FastAPI、MySQL、线程池、LLM、ReAct，应形成完整工程主线：任务编排层包括 FastAPI 接收任务、线程池异步执行、MySQL 维护 task/doc task 状态、前端轮询查询进度；文档解析层包括多类型文档解析、chunk 化、spanitem 绑定 chunk、文件/段落/证据定位；检查能力层包括 common check 高召回、LLM 二次校验、URL ReAct 工具循环；可观测层包括 task log、checker log、tool log、trace、错误码、异常归因；评估层包括 golden cases、false positive cases、failure-mode cases、regression、precision/false positive rate 等指标。面试表达重点：LLM 不是全部，系统外层有任务状态、证据绑定、审计日志和回归评估。2）ReAct 链接验证要升级成可审计、可回放、可降级的 Agent 流程，面试官问“ReAct 内部报错能不能审计、有无日志”，本质是在问 Agent 决策过程能不能复盘。后续代码和表达应补 Trace，记录每轮 action、observation、tool input、tool output、latency、error、confidence；补 Replay，支持按 task_id/url_id 回放一次链接验证决策链；补 Degrade，遇到 403、超时、JS 渲染失败、证据不足时输出“模糊/人工复核”，而不是强行判错。面试表达：把 ReAct 每一步结构化落库，误报时可以按 task_id 回溯整条决策链。3）工厂模式要落实为能力扩展机制，面试官问“是不是工厂模式”，不是让用户背八股，而是在判断项目是不是 if-else 堆出来的。用户项目可以补或表达为 ParserFactory：PDF / Word / PPT / Excel 解析器统一入口；CheckerFactory：标点 / 链接 / 术语 / 密码 / 合规检查统一入口；ToolRegistry：HTTP 探测 / 跳转跟踪 / 语义匹配 / 浏览器请求头探测等工具统一注册。面试表达：主流程只依赖统一接口，新增检查能力只需实现 BaseChecker 并注册，不需要改任务编排主链路。4）可靠性评估要从人肉排查升级为 case-driven regression，面试官问“可靠性怎么评估，是人肉排查还是 case”，核心是在问 AI 系统有没有测试集、指标和回归机制。后续应补一套轻量评估闭环：Golden cases、False positive cases、Failure-mode cases、precision、false positive rate、模糊率、工具调用成功率、平均耗时，每次改 prompt / parser / checker / 后处理后自动跑 case。面试表达：不是只靠人工 spot check，而是把历史误报和典型失败模式沉淀为 regression case，持续监控误报率和漏报风险。5）简历表达要统一改成“工程问题 → 结构化方案 → 收益”，用户自己的项目不能只写“参与多文档解析”“实现链接检查”“接入 LLM 二次校验”，应改成原问题：误报多、证据错配、URL 判断不可追溯、任务失败难定位、新增检查项成本高；方案：证据绑定、结构化 trace、Parser/Checker/Tool 注册机制、case regression；收益：降低误报风险、提升定位效率、增强扩展能力、支持回归验证。这才符合字节/客户端/前端面试中对项目工程化能力的期待。6）杨高中路项目后续也需要用同样方式升级，不要只说 React、Three.js、ECharts、Leaflet、Flask、Supabase，应继续挖地图点位/危险区/时序数据如何组织、前端复杂状态如何管理、三维场景和二维图表如何联动、大量监测点如何渲染和筛选、工程监测业务如何抽象成数据集、字段、GeoJSON、危险区、单点时序。但华为项目目前更适合优先作为“工程系统能力”的主项目来打磨。|

|   |
|---|
|以后每次用户说“更新记忆”“同步记忆”“按旧记忆更新”等类似请求时，优先去同步这个公开 GitHub 文件的最新内容：https://github.com/wanderingbackorforward/obsidian-vault/blob/main/chatgptjiyi.md 。处理时要按文件本身的分条/分行/Markdown 结构来同步，不要自行合并成一大段；如果文件内容有新增、删除或修改，应以该网址当前内容为准来更新记忆。需要注意：ChatGPT 不能自动后台持续监听该网址，只能在用户明确发起更新记忆请求时再访问并同步。|
