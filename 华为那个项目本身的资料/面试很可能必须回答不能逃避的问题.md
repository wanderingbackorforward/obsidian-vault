1
这不就是检查 URL 吗？为什么不用普通 HTTP 状态码？
原因是很多状态码本身就是有迷惑性的
例如403可能是整个不完整header 200可能是登录页 可能是首页 

2
那这个不就是ifelse状态机吗 403重试 200就语意匹配

状态机的前提是要要能够完全枚举清楚路径 但是实际上外部网页返回形式是开放的 例如200可能会存在多种情况 例如迁移页 js空壳 首页 等 我这苦lm不负责具体的状态穷举 就让llm负责自己知道错误类型 并且自己判断下一步继续访问或者提取语义或者直接来进行判断

3
那 LLM 的价值到底是什么？是不是最后做一次判断就行？

llm在最后能够确定内容后则可以不需要继续执行tools 但是在这个之前就不行 例如页面200但是让你访问新的链接等

4
你的 Agent 工具有哪些？别只说概念。
1访问页面 支持header和get
2抽取页面工具
3页面匹配工具，语义判断工具

5
Prompt 怎么写？是不是也要穷举各种网页情况？
不是 是选择失败的类型：
例如html空 例如反爬 waf 例如200但是页面不存在 例如跳转中间页
例如语言错误等

6
LLM 会不会乱调工具？比如重复请求、错误 PASS？

- action 必须在白名单里；
- 已执行过的 action 不能重复；
- 超过 max_steps 直接停止；
-

7
你具体负责了什么？
1链接对应chunk
2对于部分二次校验的流程的优化：由于内部大量术语会被判错 我设计了白名单和规则过滤 但是为了解决新术语白名单未覆盖 就是设计了相关的打分机制
3是url验证流程的设计 因为可靠性是不是只能看http状态码的 所以得把多种复杂情况抽象为failure mode 来使用reAct循环解决

8
面试官：为什么 common check 要高召回，后面再二次校验？

因为文档质量检查里，不同问题的边界不一样。如果第一轮就要求模型非常保守，很多真实问题会漏掉。

所以第一轮 common check 的目标是高召回，也就是先尽可能把疑似问题找出来。

但这样会带来误报，所以后面必须加二次校验

9
为什么不需要langchain流程呢？
langchain本质是一个LLM编排层 适合流程不固定 需要模型动态选择工具的场景 
这里首先整个大部分是固定的pipeline 只有一小部分引入了reAct循环 这小部分可以直接自己搭建 加入了增加复杂性

10
为什么不使用memory？
因为这里并不是对话助手 并不是要用来长期保持记忆 也不用保存过去的对话

11
为什么不使用rag？
